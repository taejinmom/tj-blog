package com.taejin.chat.config;

/**
 * CORS configuration is now handled by SecurityConfig.
 * This class is intentionally left empty to avoid conflicts
 * with Spring Security's CORS configuration.
 */
public class CorsConfig {
    // CORS is configured in SecurityConfig.corsConfigurationSource()
}
